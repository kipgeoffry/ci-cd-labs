pipeline {
  agent {
    dockerContainer { image 'kipgeoffry/myjenkinsagent:python' }
  }
  stages {
    stage('Check nodeJs') {
      steps {
        sh 'node --version'
      }
    }
     stage('Check hostname') {
          steps {
            sh 'cat /etc/hostname'
          }
        }
  }
}

pipeline {
  agent {
    dockerContainer { image 'node:16-alpine' }
  }
  stages {
    stage('Check python') {
      steps {
        sh 'python3 --version'
      }
    }
     stage('Check hostname') {
          steps {
            sh 'cat /etc/hostname'
          }
        }
  }
}
